export default function BindingsPage() {
  return (
    <main>
      <h1>Bindings</h1>
      <p>
        TODO: bindings[] editor (Discord channel/guild → agentId). We'll implement once we confirm what match fields you want in the UI.
      </p>
    </main>
  );
}
